# Pet repository admission — 2026-09-08

The pet task owns initial code publication and this admission candidate. The user authorized creating the plugin repository and clarified that it must be public and open source. The repository uses MIT licensing. No release or existing provider/Mono merge was requested.

The initial main revision contains repository guidance and licensing. The implementation is a separate development PR: it consumes experimental Host PR 349 and Protocol PR 119 and cannot yet be treated as a formal compatible product baseline. No existing Mono gitlinks are advanced by this admission.

The shop/progression proposal is deferred in the owner repository issue; Avatar's existing skin capability is reused. This record contains no duplicate product specification.

Validation and remote references are recorded in the admission PR. Formal admission remains pending review, authorized merge and remote readback. Public registration must not retain the private `update = none` setting; only existing private mounts are skipped in anonymous retrieval.

Network-blocked checkpoint: owner main ddb3d2090f3c23212fc0a339e7291a8097c8a874 (tree e84e86494b6008cca2fd714761c823a4bfd1bfd7) was pushed and is the selected bootstrap pin. Development commit 15f3a58 was pushed; local license and package-license changes await successful push. Requested public visibility is not yet confirmed after repeated TLS failures; this candidate must not merge until remote visibility, licensing, anonymous recursive retrieval and inventory reconciliation are verified. Owner issue: https://github.com/cordisx/plugin-pet/issues/1 . Plugin check14 passed against the relative sibling Host dependency; registry tests39 passed before staging the new pin and are repeated below.
