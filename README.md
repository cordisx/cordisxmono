# CordisXMono

Unified CordisX workspace assembled from independently versioned repositories.

CordisXMono does not own product code. Repositories are mounted as Git submodules under `vendors/<org>/<repo>`.
